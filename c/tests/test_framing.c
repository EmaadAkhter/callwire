#include "callwire.h"
#include <stdio.h>
#include <string.h>
#include <assert.h>

/* Stub test for framing layer. Real tests would use mocked sockets or pipes. */

int main(void) {
    printf("callwire framing smoke test\n");
    /* TODO: add real framing tests once socket mocking is in place */
    return 0;
}
