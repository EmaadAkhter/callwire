{
  "name": "@emaad-ansari/callwire",
  "version": "2.1.0",
  "description": "High-performance, bidirectional RPC over TCP with MessagePack framing — TypeScript/Node.js client & server",
  "main": "dist/index.js",
  "bin": {
    "callwire": "dist/cli.js"
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "pretest": "python3 -m pip install --break-system-packages -e ../python",
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "rpc",
    "msgpack",
    "callwire",
    "tcp",
    "ipc"
  ],
  "author": "Emaad Akhter",
  "license": "MIT",
  "dependencies": {
    "@msgpack/msgpack": "^3.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.0.0",
    "typescript": "^6.0.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testTimeout": 60000,
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
